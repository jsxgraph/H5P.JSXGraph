/*
const ajaxURL =
document.location.href.split('?').shift().replace('admin.php', 'admin-ajax.php');

H5P.jQuery.ajax({
    url: ajaxURL,
    type: 'get',
    data: {
        action: 'h5p_content_results',
        id: '1'
    },
    success: (results) => {
        console.log(results);
        console.log(H5PIntegration.user);
    }
});
*/